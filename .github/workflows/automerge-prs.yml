name: Automerge PR

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: tkhom3/gha-workflows/.github/workflows/automerge-prs.yml@35425a1e4fcba9bc77df1cefcdc099ba34ce9fa1 # v1.0.29
    secrets:
      token: ${{ secrets.GHA_PAT }}
